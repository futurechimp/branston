<h1>New iteration</h1>

<% form_for(@iteration) do |f| %>
  <%= f.error_messages %>

   <%= render :partial => 'form', :locals => {:f => f } %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', iterations_path %>

