<% @page_title = "Manage Projects" %>
<h2><%=h @page_title %></h2>

<div id="actions">
	<ul>
		<li><%= link_to 'New Project', new_project_path, :class => 'add' %></li>
	</ul>
</div>

<table class="table">
  <tr>
    <th>&nbsp;</th>
		<th>&nbsp;</th>
		<th>&nbsp;</th>
  </tr>

  <% for project in @projects %>
  <tr class="<%=cycle('odd','even')%>">
    <td><%= link_to project.name, project, :title => project.description %></td>
    <td class="action"><%= link_to 'Edit', edit_project_path(project) %></td>
    <td class="action"><%= link_to 'Destroy', project, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
  <% end %>
</table>


