<h2>New story</h2>

<% form_for(@story) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => {:f => f} %>

  <p class="clear-left">
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', stories_path, :class => "back" %>

